package com.club.soccer.domain

enum class MatchStatus { SCHEDULED, LIVE, FINISHED, CANCELLED }
enum class ReservationStatus { PENDING, EXPIRED, CONFIRMED, CANCELLED }
enum class PaymentStatus { PENDING, SUCCESS, FAILED, CANCELLED }
